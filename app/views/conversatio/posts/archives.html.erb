<h1><%= @blog.title -%></h1>

<% if @posts.empty? %>
  <p>has no blog posts in the specified date.</p>
<% else %>
  <h2>Archive of <%=@year %>/<%= @month unless @month.nil? %><%= "/#{@day}" unless @day.nil? %></h2>

  <% @posts.each do |post| %>
    <div id="post_<%= post.id %>" class="border_top">
      <h2><%= link_to "#{sanitize textilize(post.title)}", conversatio_blog_post_path(@blog, post), :title => "#{sanitize post.title}" %></h2>
        <div>Posted <%= time_ago_in_words post.created_at %></div>
        <%= render :partial => 'conversatio/posts/tags', :locals => { :post => post } %>

        <div><%= sanitize textilize(post.body) %></div>
        <div id="border_top">
          <%= "Comments (#{post.comments.size})" %> | <%= link_to 'Add a Comment', conversatio_blog_post_path(@blog, post), :title => "Leaving A Comment On #{h post.title}" %>
        </div>
    </div>
  <% end %>
<% end %>

<%= render :partial => 'conversatio/blogs/tag_cloud', :locals => { :blog => @blog } %>

Archives:
<%= archives_menu %>